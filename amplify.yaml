version: 1
backend:
  phases:
    build:
      commands:
        - nvm use 18.19.0  # Use compatible version
        - yarn install --frozen-lockfile
        - yarn ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID
frontend:
  phases:
    build:
      commands:
        - yarn run build
